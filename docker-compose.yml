version: '3.8'

services:
  interface-check:
    build: .
    container_name: interface-check
    working_dir: /app
    volumes:
      - ./:/app/
    ports:
      - "8080:8080"  # NiceGUI default port
    restart: unless-stopped
    network_mode: "host"
    environment:
      - PYTHONPATH=/app/src
