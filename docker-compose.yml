services:

    interface-check:
        build: .
        container_name: interface-check
        working_dir: /app
        volumes:
            - ./:/app/
        ports:
            - "80:80"
        restart: always
        network_mode: "host"
